🚨 SUPREME DIRECTIVE: VERSION CONTROL LOCK (v28.33 - Ironclad CADES (80% Trail Lock) - Active) 🚨
CRITICAL AI INSTRUCTION: Purge all prior architectures. Strictly obey v28.33. This is a CODE FREEZE. Do not modify core logic without SRE authorization. A trade must survive all FIVE WALLS and DIRECTIVE OMEGA to be executed.

WALL 0: MODEL ORIGINATION & BACKTESTING [Anti-Curve Fitting]
* Walk-Forward Validation ONLY: Models MUST be trained using expanding or rolling chronological windows. Random K-Fold splits are strictly forbidden due to serial correlation. Only Out-Of-Sample (OOS) metrics may be reported.
* The Reality Tax: All backtests MUST mathematically deduct `1.0x spread` for slippage, plus flat commissions, plus daily overnight swap/carry costs. If a strategy lacks edge after the Reality Tax, it is discarded.
* Zero Look-Ahead Bias: `feature_engineering.py` is strictly forbidden from using negative shifts (`.shift(-n)`), backward fills (`.bfill()`), or future-peeking roll windows. Enforced by static AST scanning.
* Rigorous Quantitative Metrics: All strategy evaluation metrics MUST use strict institutional mathematics. Returns must be calculated relative to allocated capital (not entry price). Sharpe and Sortino ratios MUST be annualized using correctly resampled daily data, never per-trade data. Drawdown must be calculated geometrically (peak-to-trough percentage). Metric outputs must remain as raw floats for optimization algorithms.
* Sequence of Returns Risk: All backtests must pass a Monte Carlo permutation test (1,000 shuffles). If the median shuffled Maximum Drawdown exceeds 15%, or if the original backtest sequence was in the top 10% luckiest distributions, the strategy is deemed path-dependent and must be discarded.

WALL 1: ARCHITECTURAL & SIGNATURE INTEGRITY [Anti-Ghosting]
* Singular RAM Dominance: Orchestrator MUST perform an OS-level sweep to kill legacy daemons before boot.
* Port Liberation: Ports 8000 and 8001 must be strictly unbound.
* Universal UTF-8: All logs MUST use `logger_config.py`. Set `PYTHONIOENCODING=utf-8` and `sys.stdout.reconfigure(encoding="utf-8")`.
* Absolute Signature Mandate: Hardcoded versions are forbidden. Dynamically import `AGENT_SIGNATURE` from `sentinel/version_manifest.py`. MT5 comments MUST be < 31 chars and match the signature exactly.
* CI/CD Self-Cert: `self_cert.py` must pass; git pre-commit hooks must block legacy strings.
* Continuous Temporal Indexing: Dataframes MUST forward-fill price and zero-fill volume for broker maintenance gaps prior to feature engineering.
* Information-Driven Heartbeat: The Slow Loop MUST NOT lock the main thread with long chronological sleep periods (>60s). It must utilize event-driven polling or volume/volatility thresholds to trigger intra-bar evaluation during macro shocks, while always executing a mandatory cycle immediately upon boot.
* Silent Fallback Veto: Missing model weight files (.json, .pt, .pkl) MUST trigger a hard [SRE HALT] and exit. The system is strictly forbidden from defaulting to 0.50 or dummy logic to keep the pipeline running.
* Stale Cache Veto: Daemons reading from the database must verify timestamp freshness. Reading a stale cache value and writing it back unchanged to simulate a live cycle (Feedback Locking) is strictly forbidden.
* The Native Broker Time Mandate: The system MUST NEVER calculate position holding durations or decay timers using Python's local time.time(). All time-elapsed math must query the native broker clock via mt5.symbol_info_tick(symbol).time or mt5.symbol_info(symbol).time to prevent timezone offset loops.
* Continuous Fuzzing Mandate: The system must run an independent sre_watchdog.py daemon that periodically tests the execution API with corrupted, degraded, and out-of-bounds payloads. If the API ever accepts a poisoned payload, the Watchdog must instantly terminate all trading processes.
* Execution Deduplication Mandate: To prevent broker API throttling and rate limits, the Execution Router and Profit Manager MUST utilize Execution Deduplication. The system is strictly forbidden from dispatching a TRADE_ACTION_SLTP modification if the calculated target SL/TP is identical to the current physical SL/TP resting on the MT5 server.
* State Isolation Mandate: Global State Pollution is strictly forbidden. Any evaluation anomaly (e.g., tick starvation, NaNs, missing models) must be contained to a LOCAL variable (is_this_symbol_starved) specific to that asset's thread. A symbol's localized failure MUST NOT mutate a global fallback flag unless that symbol belongs to the CORE_MAJORS array (representing systemic broker failure).

WALL 2: EPISTEMIC & TOXIC GATING [Anti-Noise & Anti-Drift]
* Model Edge Baseline: All active models (XGBoost, DDQN) MUST maintain an Out-of-Sample AUC-ROC > 0.55.
* Model Drift Telemetry: The system MUST monitor the rolling standard deviation of the last 100 Alpha Combiner $P$ scores. If `std < 0.05`, the models have suffered mode collapse. The system MUST halt autonomous trading. Extreme conviction ($P > 0.85$) is mathematically capped to occur in < 15% of signals. Drift Telemetry MUST strictly measure the variance of RAW model predictions. Any signals mathematically forced to 0.50 by structural safety gates (like Divergence or Overconfidence vetos) MUST be excluded from the rolling standard deviation calculation to prevent false-positive halts.
* Feature Integrity Assertions: All engineered dataframes MUST pass strict mathematical bounds checks (e.g., RSI clamped to [0,100], strictly positive ATR) via hard assertions before ML inference.
* Agent Quarantine (Consensus Purity): Uninitialized/untrained agents artificially dilute conviction. All models MUST pass through the `agent_quarantine.py` registry.
* Sealed Hysteresis: Ensemble scores of 0.40 <= P <= 0.60 are HARD BLOCKED as phantom conviction.
* Combiner Integrity: Cross-sectional asset scoring MUST utilize `NaN`-safe math (`np.nanmean`, `np.nanstd`).
* Toxicity Blindness Veto: The system MUST evaluate VPIN/Order-Flow Toxicity. Trades firing into highly adversarial flow must be rejected.
* Correlated Overconfidence Veto: If >20% of the watchlist generates extreme conviction ($P>0.90$ or $P<0.10$) simultaneously, all signals must be vetoed as a model hallucination. Divergence gating must dynamically expand to 0.40 only during valid extreme conviction events.
* Strict Numerical Sanitization (The Bounds Mandate): All feature engineering, Z-score standardizations, and mathematical outputs MUST use safe division padding AND strict bounds clipping (e.g., np.clip(val, -3.0, 3.0)). Floating-point precision overflow (e.g., Entropy = 1.00000001) that violates bounds checks will result in immediate execution vetoes.
* Floating-Point Clamp Rule: All statistical bounds, particularly Shannon Entropy and Z-Scores, must be explicitly wrapped in np.clip (e.g., [0.0, 1.0]) to protect the primary inference cycle from floating-point overflow crashes.
* Core Major Failsafe Rule: The system must maintain a hardcoded set of CORE_MAJORS (e.g., EURUSD, SP500, XAUUSD). Only starvation or data degradation on a Core Major is permitted to trigger a global divergence clamp or global execution halt. Minor assets and altcoins must fail silently and be skipped without affecting the global router.
* Calibration Mandate: Deep Learning Oracles are strictly forbidden from operating uncalibrated. All raw logits must pass through Temperature Scaling to prevent Softmax Saturation. Any individual model outputting extreme confidence (>0.95 or <0.05) must be treated as saturated/hallucinating, immediately quarantined for that evaluation cycle, and stripped from the P_blend consensus.

WALL 3: REGIME ALIGNMENT [Anti-Decay]
* Double Mutex Amnesia Lock: 86,400s (24 Hours) post-exit embargo to prevent noise chop.
* Regime Misalignment Veto: The strategy must mathematically align with the environment. Mean-reversion signals in a TREND regime, or Momentum signals in a RANGE regime, MUST be explicitly rejected by the execution bridge.
* HMM Calibration: The HMM Regime Classifier MUST maintain >70% accuracy against structural Ground Truth (ADX > 25).
* Heuristic Override: If legacy ML models crash due to H1/H4 dimension mismatch, route raw Swing Alpha signals directly to execution via hardcoded scores.
* Timeframe Matching: Thesis Decay exits are STRICTLY FORBIDDEN before the asset-specific minimum hold time has elapsed (e.g., 12 hours for Swing). Conviction ($P$) scoring for exits MUST match the original entry timeframe (H4 entries use H4 conviction).

WALL 4: STRUCTURAL MATH & EXECUTION [Anti-Slippage]
* ZERO NAKED TRADES: Execute via ECN 2-Step wrapped in an atomic retry block (max 3 retries).
* Naked Kill Switch: If `TRADE_ACTION_SLTP` fails 3 times, OR Profit Manager finds `SL == 0` for > 10 seconds, INSTANTLY liquidate via Market Close.
* Risk Agent Circuit Breaker (FAIL-CLOSED STRICT): If Port 8001 times out or errors, the Fast Loop MUST explicitly reject the trade by raising `TradeRejected`.
* Spread-Aware Armor: Baseline risk distance MUST enforce a safety floor: `true_atr = max(H1_atr, price * 0.0025, 1.5 * current_spread)`.
* Fractal Anchoring: MT5 SL = `max(3.0 * true_atr, distance_to_Fractal_SL)`. All SL/TP must pass a strict `trade_tick_size` normalization wrapper.
* ATR-Adjusted Sizing: Max dollar risk = 2%. Final lot is the lesser of the Kelly-Fraction lot and the ATR-Adjusted lot. All position sizing must strictly adhere to Target Volatility scaling. Lot sizes must be inversely proportional to the asset's current Average True Range (ATR) relative to its historical baseline. The system must automatically slash exposure during volatility expansions to maintain constant portfolio heat.
* Strict Tick-Size Alignment Rule: All calculated prices (Entry, SL, TP) must be strictly rounded to the asset's specific exchange trade_tick_size before being sent to the MetaTrader 5 terminal to prevent floating-point order rejections.
* Split-Brain calculation is forbidden. The Execution Router (Fast Loop) MUST NOT independently calculate entry thresholds, dynamic gates, or regime adjustments. The Alpha Factory (Slow Loop) is the Single Source of Truth and MUST package the exact applied_dynamic_gate inside the signal payload. The Execution node must blindly evaluate norm_p >= applied_dynamic_gate.

WALL 5: EX-ANTE MACRO SHIELD [Anti-Shock]
* The Carry Tax: Risk Agent must query `swap_long/short`. Veto the trade if the overnight carry cost > 10% drag on the Zone 2 TP over 7 days.
* Daily Rollover Blackout: NO new trades may be opened between 23:55 and 00:15 Broker Time to avoid catastrophic exotic spread slippage.
* Ex-Ante Event Blackout (STRICT): Query macro/earnings calendars. Macro blackouts MUST be liquidity-tiered. Tier 1 assets (Majors/Indices/Gold) require a 12-hour blackout. Tier 2 (Minors) require an 18-hour blackout. Tier 3 (Crosses) default to a 24-hour blackout. A blanket 24-hour global blackout is forbidden as it causes uptime death spirals. Pre-scaling or reducing lot sizes for new entries is explicitly forbidden.
* 12-Hour Event Horizon: Active trades within 12 hours of a Tier-1 event must scale out 50% and move SL to breakeven.
* True Swing Time-Stop: Dead-money exit after 72 H1 Candles (pause countdown Friday 23:55 to Monday 00:15). Adaptive exits at Zone 1 (+2.5 ATR), Zone 2 (+4.0 ATR), Zone 3 (+5.0 ATR Trail).

DIRECTIVE OMEGA: IRONCLAD ENTRY VETO FRAMEWORK [v28.14+ SUPREME MANDATE]
1. ZERO-SIZING ABSOLUTE VETO (Rule 1.1): Small Account Floor is abolished. Rounding 0.0 lot size to 0.1 is strictly forbidden. If risk calculation returns 0.0, trigger ZERO_SIZING_VETO and abort.
2. AFFORDABILITY PRE-SCREEN (Rule 1.2): Before generating signals for Indices/Metals/Crypto, check affordable_lot = risk_budget / (atr * point_value * 3.0). If < broker_min_lot, trigger AFFORDABILITY_VETO and skip signal generation.
3. DEGRADED DATA ENTRY VETO (Rule 2.1): If the pipeline triggers M1 fallback, anti-starvation loops, missing ML modules, or Z-score clipping on >3 features, flag data_quality_flag != "PRISTINE" and abort trade.
4. DIVERGENCE TIGHTENING (Rule 2.2): Dynamic divergence gate tightening (from 0.30 to 0.15) and data degradation overrides MUST be strictly isolated to the specific starved asset to prevent false-positive trading freezes. Global tick starvation overrides are strictly prohibited from being triggered by minor or illiquid altcoins, and must only engage if core majors or indices suffer tick gaps.
5. CONVICTION QUALITY GATE (Rule 3.1 & 3.2): Raise P_blend minimums to >= 0.68 (Standard), >= 0.72 (High-Vol Indices/Metals), or >= 0.75 (Degraded Sessions). Reject signals if Kronos < 0.70 OR XGBoost < 0.65 (normalized to signal direction). Absolute static gates are forbidden. All Epistemic Gates MUST be dynamically adjusted based on the Volatility Regime Score (VRS). During low volatility (VRS < 0.8), gates are discounted by -0.015 to account for mathematical score compression. During high volatility (VRS > 1.2), gates are penalized by +0.020 to demand extreme conviction. No gate shall ever drop below 0.65.
6. REGIME MINIMUM (Rule 3.3): Reject signal if HMM Regime Probability for predicted direction is < 0.60.
7. ZERO-MFE PREVENTION (Rule 4.1): Check last 5 ticks. For BUY, (last_tick - first_tick) must be > 0. For SELL, must be < 0. Soft delay on fail; HARD_VETO if unconfirmed.
8. SPREAD-TO-ATR RATIO (Rule 4.2): Block trade if spread/atr > Limits (GER40/HK50: 0.025, US30/NAS100: 0.020, XAUUSD: 0.015, default: 0.030).
9. MINIMUM RISK-TO-REWARD GATE (Rule 4.3): Reject if tp_distance / sl_distance < 1.8 (or < 2.2 for High-Vol Indices).
10. COMPOSITE PRE-FLIGHT CHECKLIST (Layer 5): Rigorously execute a 20-point boolean checklist before any mt5.order_send. Any single FAIL triggers an instant HARD_VETO. All 20 pre-flight execution gates MUST use explicit type-casting (float(), str()) and explicit comparison operators (== True, != 'PRISTINE'). Python's implicit truthiness evaluation is strictly forbidden in the execution router.
11. INSTRUMENT-CLASS RULES (Layer 6): Indices apply Zero-Sizing unconditionally and block all indices on one index starvation. JPY pairs tighten velocity kill to -0.20 and block openings (Tokyo/London/NY). Metals block entries 15 mins before/after USD releases.
12. ACCOUNT EQUITY CIRCUIT BREAKERS (Layer 7): Block all entries for the day and tighten SL to 0.5 ATR if daily drawdown >= 3.0% (FORTRESS_MODE). Execute 4-hour HARD_PAUSE if consecutive losses >= 3. Permanently block High-Vol Indices if equity < 2000. Rule 7.4 - The Correlation Cap: The total aggregated directional risk against any single currency (e.g., net long USD or net short JPY across all open pairs) MUST NOT exceed 4.0% of account equity. Trades that breach this limit must be blocked via a PORTFOLIO_HEAT_VETO to prevent correlated systemic wipeouts. Systemic performance and Sharpe ratios must be evaluated using R-Multiples (Risk-Adjusted Return), not raw dollar P&L, to ensure the evaluation of the algorithmic edge remains untainted by dynamic position sizing.
13. ADAPTIVE POSITION MANAGEMENT (Layer 8): It is constitutionally forbidden to alter a Stop Loss while a trade is in negative floating P&L (in the red). Swing trades must be given total freedom to reach their original physical Stop Loss. The Profit Manager may ONLY engage a trailing stop if the position is functionally in profit AND has achieved >= 80% of the distance to its Take Profit target. Lock Breakeven at +0.5 ATR. Partial scale 35% at Zone 1 (+1.5 ATR) and 35% at Zone 2 (+2.5 ATR). Trail remainder at Zone 3 (+4.0 ATR). Re-evaluate conviction every 2 hours and exit if live conviction drops > 0.12 from entry. All dynamic Stop Loss modifications—including trailing stops, breakeven locks, and especially Fortress Mode adverse risk-reduction trailing—MUST dynamically query the broker's trade_stops_level and live spread. The target SL must be validated against the safe boundary: (stops_level * point) + spread + (2 * point). Any SL falling inside this prohibited freeze zone MUST be mathematically clamped to the safe boundary prior to execution to absolutely prevent MT5 retcode=10016 rejections. Continuous daemon loops (like the Profit Manager) must not continuously poll the Slow Loop or database for P_blend (Thesis Decay) scores at 1Hz. They must implement an OracleCache that stores the latest inference state and only refreshes asynchronously when the 5-minute candle closes, preserving CPU and I/O bandwidth.
