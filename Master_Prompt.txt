⚙️ SYSTEM COMMAND: Adaptive Sentinel Execution & Risk Audit (v16.9 - Production Build)
Phase 1: Architecture, Synchronization & Scalability
Information-Driven Architecture (Dollar/Volume Bars): The system strictly rejects chronological Time Bars (e.g., 5-minute charts). The Dual-Loop matrix operates on an Event-Driven basis. Both the Slow Loop (sentinel_slow_loop.py) and the Fast Loop (chat_gemma.py) only trigger inference and execution evaluations when a statistically significant threshold of fiat value is exchanged in the market (Dollar Bars). The system natively sleeps during low-volume noise and hyper-samples during high-volume volatility.
Symbol Auto-Discovery: Do not hardcode broker suffixes. sentinel_config.py must utilize a fallback base-symbol matcher to dynamically ping MT5 and map the base watchlist to exact tradeable strings.
Universal UTC & Staleness: Enforce a hard 300ms timeout on ArcticDB reads to accommodate the 56-asset concurrency load. All timestamps written/read from the cache must use universal UTC epoch time. Verify cached signal age; if > 360 seconds old, halt new entries for that asset and log [STALE_SIGNAL].
Data Preprocessing & Fractional Differentiation (FracDiff): The Slow Loop must never use integer differentiation (pct_change() or .diff()) to achieve stationarity, as this destroys the market's memory. All price series fed into the ML models (XGBoost/TimesNet) must be Fractionally Differentiated. The pipeline must dynamically calculate the optimal fractional $d$ value (between 0 and 1) that satisfies the Augmented Dickey-Fuller (ADF) test ($p < 0.05$) while preserving maximum correlation to the raw price series.
Phase 2: Perception & Cognition (Meta-Labeling & White-Box Oracles)
HMM Oracle: Poll the active Hidden Markov Model state (BULL, BEAR, RANGE). Ensure dynamic standard deviation scaling is used for regime boundaries.
Meta-Labeling Architecture (Decoupled Sizing): The system strictly separates directional forecasting from position sizing.
Primary Model (Direction): The base Oracles (XGBoost/TimesNet) dictate ONLY the side of the trade ($1$ for Long, $-1$ for Short).
Meta-Model (Conviction): A secondary machine learning classifier outputs the final conviction probability ($p$).
Native MoE Reasoning & Thinking Budgets: Local inference must utilize Mixture-of-Experts logic (e.g., Qwen-A3B) to maintain high IQ on CPU hardware. The Slow Loop must enforce native reasoning (enable_thinking=True) before generating conviction scores. A strict ThinkingBudget must be applied to cap reasoning length, preventing local CPU lockups. All outputs must be sanitized using strict JSON Schema retry loops.
SHAP Feature Importance (The White Box): Machine learning models must never operate as black boxes. For every prediction, the Slow Loop must utilize shap.TreeExplainer to calculate the marginal contribution of every feature. The top 3 positive and negative drivers must be formatted into a JSON payload and dropped into shap_diagnostics/ for real-time SRE auditing.
Concurrency Sample Weighting (IID Fix): The Slow Loop must calculate the "Average Uniqueness" (concurrency) of every training label. Highly overlapping labels must be penalized with fractional weights ($1 / c_t$) to prevent overfitting.
The Epistemic Gate (0.82 Threshold): The Fast Loop must enforce an absolute minimum conviction threshold of 0.82 based strictly on the Meta-Model's output.
Regime Alignment: If HMM == BEAR, strictly block all BUY signals. If HMM == BULL, strictly block all SELL signals.
Phase 3: Contextual Memory Audit (FAISS)
FAISS Index: Query the RAM-loaded FAISS EpisodicMemory (Dim=93).
Legend Override (The Winners): If the live vector exhibits > 85% cosine similarity to a legend_wei template, bypass all Phase 2 HMM penalties, grant clearance, and overwrite $p$.
The Graveyard (The Losers): The Fast Loop must dynamically query the FAISS index against recent post_mortem_failure vectors. If a live setup exhibits > 85% cosine similarity to a cluster of past failed trades, the trade is autonomously blocked.
Phase 4: Risk Gates & Fractional Kelly Sizing
The Amnesia Lock: Query MT5 for existing positions (magic=142). If a position exists in the same direction, DO NOT execute new sub-orders.
Asset-Aware Weekend Protocol: Forex, Indices, and Equities must be blacked out from Friday 23:55 to Monday 00:15 Broker Time. CRITICAL: Crypto markets are 24/7. The Slow Loop, Fast Loop, and Orchestrator MUST remain active over the weekend exclusively for Crypto assets. If ArcticDB cache data is > 300 seconds stale, default to HOLD.
Fractional Kelly Sizing (CRITICAL): Calculate raw Kelly: $f^* = p - (q / b)$. The probability ($p$) must be sourced exclusively from the Phase 2 Meta-Model. Multiply $f^*$ by KELLY_FRACTION = 0.25.
Absolute Risk Ceilings: Enforce Portfolio Heat (<=20%), Hard Risk Cap (<=2.0%), and the Leverage Wall (<=10x equity margin limit).
Phase 5: Signal Handoff, MCP Execution & SRE Watchdogs
Local-Only SRE Orchestration (Zero-Cost Antigravity): The Hermes SRE Agent and overall Orchestrator MUST run exclusively on the local TurboQuant/Ollama endpoint (http://127.0.0.1:8080/v1). The use of Cloud LLMs (OpenRouter/Gemini/DeepSeek) for base SRE operations is strictly prohibited to prevent API billing failures and ensure 100% offline capability.
MCP Execution Firewall (System Harmony): The Orchestrator interacts exclusively through the trade_executor_mcp.py tool, pulling all hyperparameters dynamically from the global_hyperparameters ArcticDB Feature Store.
Hybrid Catastrophe Stops (Offline Protection): To survive local infrastructure failures (e.g., laptop closing, Wi-Fi drops), purely "Virtual" stops are strictly prohibited for base risk management. The Executor MUST calculate and transmit a hard, physical Stop Loss price (based on the VectorBT-optimized Asset-Class ATR multiplier) directly to the MT5 broker on every sub-order entry.
Alternative Data Oracle (Black Swan Shield): The Orchestrator autonomously invokes fetch_unstructured_sentiment. If real-time sentiment scores below -0.60 (Long) or above +0.60 (Short), trigger Virtual Exit.
Active Position Management & Physical Trailing: An Asynchronous Profit Manager evaluates Regime Inversions and Conviction Decay for early Virtual Exits. However, for trailing stops, it MUST use mt5.order_modify to physically drag the broker-side Catastrophe Stop deep into profit, ensuring gains are locked on the broker server if the connection is lost. CRITICAL: If a trade hits a stop loss, embed the market context into a 93-D vector and write to the FAISS index as a post_mortem_failure.
Constitution Audit & PSR Tripwire: The Profit Manager strictly audits live trades. It must calculate the Probabilistic Sharpe Ratio (PSR) on the live equity curve, injecting real-time skewness and kurtosis. If the live PSR drops below 0.80, the system must autonomously drop a PSR_DEGRADATION JSON into pending_diagnostics/ to trigger an immediate SRE halt and VectorBT re-optimization.
ChatOps, SHAP Diagnostics & Autonomous SRE (Hermes): Hermes polls pending_diagnostics/ to autonomously patch underlying execution scripts. CRITICAL (Concept Drift Monitor): Hermes must continuously monitor shap_diagnostics/. If any single feature accounts for $> 65\%$ of a model's predictive weight, Hermes must flag a CONCEPT_DRIFT_WARNING, artificially force the Meta-Model conviction to $0.0$ (blocking execution), and alert the user via Discord.
Autonomous Quant Researcher (VectorBT, CPCV & DSR): Hermes combats parameter decay via asynchronous sweeps. All backtests MUST use Combinatorial Purged Cross-Validation (CPCV) with strict Purging and Embargoing. Hermes must calculate the Deflated Sharpe Ratio (DSR) to explicitly discount performance by the total number of parameter combinations tested. Trigger a Discord webhook ONLY if DSR > 0.95. Upon user approval, Hermes writes the parameters to ArcticDB.
P5 Circuit Breaker: If live drawdown >= 15%, throw EMERGENCY_LIQUIDATION=True, close all open sub-orders, cancel pending limits, and lock the terminal for 24 hours.
Phase 6: Constitution Maintenance (Self-Updating Protocol)
The Living Document: This Master Prompt serves as the strict constitution for the Adaptive Sentinel matrix.
Forced Overwrite: Anytime you (the AI coding agent) implement a new feature, fix a bug, or adjust a core mathematical threshold, you MUST automatically update this constitution to reflect the new architecture. Output the fully updated text and explicitly command the user to save/overwrite it at C:\Sentinel_Project\Master_Prompt.txt.
