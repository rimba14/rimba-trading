🚨 SUPREME DIRECTIVE: VERSION CONTROL LOCK (v19.8 - Strict Net Exposure) 🚨
CRITICAL AI INSTRUCTION: This document is the absolute, unquestionable authority for the Adaptive Sentinel architecture. Purge all prior architectures (v1.0 through v19.7). Strictly obey v19.8.
Phase 1: Architecture, Synchronization & Scalability
Event-Driven Architecture: The system rejects Time Bars. Inference is triggered by fiat value thresholds.
Absolute State Isolation: sentinel_slow_loop.py MUST process assets via asyncio.gather(). All feature vectors MUST be instantiated from scratch INSIDE each coroutine.
ASCII Auditing: Conviction scores logged at 6-decimal precision. All terminal logging MUST be strictly ASCII-compliant to prevent Windows charmap crashes.
OOM Prevention: 1. Hard-cap PyTorch threads via torch.set_num_threads(4). 2. Pre-quantize models offline; live bridges are strictly "Load-Only" (.pt files > 1MB). 3. Aggressive garbage collection (gc.collect()) after model I/O.
Phase 2: Perception, Cognition & Self-Evolving Execution
Gemini Deep Research Oracle: Background process outputs macro_state.json every 24h.
Mathematical Meta-Model & Soft Scaling: Meta-Model calculates Conviction ($P$).
Soft Confidence Blend: MoE reasoning applies a soft floor: strength = max(0.6, reasoning_conf) to preserve the ML signal (p_final = 0.5 + (base_p - 0.5) * strength).
I/O Hardening: .pkl Meta-Model MUST be loaded using joblib.load() within a try/except block catching I/O race conditions.
SHAP Diagnostic Hardening: Explicitly cast feature vectors to 2D matrices (reshape(1, -1)). Set check_additivity=False.
Black Swan Override: If black_swan_risk > 0.85, force $P=0.0$ and broadcast global LIQUIDATE ALL.
Phase 3: Contextual Memory Audit (FAISS SQ8)
SQ8 Episodic Memory: Query RAM-loaded FAISS utilizing INT8/SQ8. Live vectors remain FP32 for query execution against Legend/Graveyard templates.
Phase 4: Dynamic Risk Gates & Hyperopt Sizing
Optuna Bayesian Engine: Continuously hunts for optimal Epistemic Gate (e.g., > 0.82) and Kelly Fraction parameters.
The Amnesia & Mutual Exclusion Lock (CRITICAL): The system operates on STRICT NET EXPOSURE (No Hedging). Query MT5 for existing positions. If a position exists in the same direction, DO NOT execute new sub-orders. If a position exists in the opposite direction, the system MUST autonomously liquidate the opposite position before executing the new signal.
Phase 5: Decoupled Bridge & Virtual Execution
The Cognitive Engine: Runs EXCLUSIVELY as headless daemon processes. Format signals as JSON payloads pushed via HTTP POST to the local Execution node.
Autonomous Profit Manager: Execution scripts MUST NEVER attach physical SL/TP orders to the broker. profit_manager.py MUST run as an autonomous, endless background polling loop. If a Long's $P \le 0.48$ or a Short's $P \ge 0.52$, the manager MUST instantly and autonomously fire a liquidation payload without any human-in-the-loop authorization.
Phase 6: Constitution Maintenance
Scorched Earth Rule: Autonomously DELETE older versions of this prompt to prevent RAG contamination. There can be only ONE Master_Prompt.txt.
