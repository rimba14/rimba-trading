⚙️ SYSTEM COMMAND: Adaptive Sentinel Execution & Risk Audit (v15.0 - Production Build)
Phase 1: Architecture, Synchronization & Scalability
Dual-Loop Concurrency: Acknowledge the asynchronous environment. The Slow Loop (sentinel_slow_loop.py) manages heavy Oracle inference and temperature scaling on a 5-minute cycle using a ThreadPoolExecutor (max 5 workers). The Fast Loop (chat_gemma.py) continuously evaluates the multi-asset matrix using a ThreadPoolExecutor (max 10 workers) to prevent OOM crashes.
Symbol Auto-Discovery: Do not hardcode broker suffixes. sentinel_config.py must utilize a fallback base-symbol matcher to dynamically ping MT5 and map the base watchlist to exact tradeable strings (e.g., matching XAUUSD.v to XAUUSD).
Universal UTC & Staleness: Enforce a hard 300ms timeout on ArcticDB reads to accommodate the 56-asset concurrency load. All timestamps written/read from the cache must use universal UTC epoch time. Verify cached signal age; if > 360 seconds old, halt new entries for that asset and log [STALE_SIGNAL].
Zero-Latency Data Types: Parse ArcticDB cache data into native Python dictionaries/dataclasses. No pandas/numpy processing in the Fast Loop.

Phase 2: Perception & Cognition (The Oracles & Epistemic Gates)
HMM Oracle: Poll the active Hidden Markov Model state (BULL, BEAR, RANGE). Ensure dynamic standard deviation scaling is used for regime boundaries (e.g., > 1.5 Sigma for strong directional moves) rather than fixed percentages.
Volatility-Adjusted Scaling (Write-Side ONLY): To prevent the Micro-Variance Squeeze (the 0.50 collapse), all raw model logits from Kronos/TimesNet must be normalized against recent ATR. Apply a TEMPERATURE = 3.0 multiplier strictly on the Write-Side (inside the Slow Loop bridges) before passing through the final sigmoid. CRITICAL: Do NOT apply any secondary temperature scaling on the Read-Side (Fast Loop).
The Epistemic Gate (0.82 Threshold): The Fast Loop must enforce an absolute minimum conviction threshold of 0.82. Any asset scoring below this is immediately rejected with verbose telemetry.
Override & Blending Rules: Blend Kronos with the XGBoost baseline at a 70/30 weight to determine final probability ($p$). Kronos has Full Override authority (100% weight) ONLY IF: (1) Divergence with XGBoost is $\le 0.30$, (2) HMM state is NOT 'RANGE' or 'VOLATILE', and (3) Volume $> \text{20th percentile}$.
Regime Alignment: If HMM == BEAR, strictly block all BUY signals. If HMM == BULL, strictly block all SELL signals.

Phase 3: Contextual Memory Audit
FAISS Index: Query the RAM-loaded FAISS EpisodicMemory (Dim=93).
Legend Override: If the live vector exhibits > 85% cosine similarity to a legend_wei template, bypass all Phase 2 HMM penalties, grant execution clearance, and overwrite $p$ with the matched template's historical win-rate.

Phase 4: Risk Gates & Fractional Kelly Sizing
The Amnesia Lock: Query MT5 for existing positions (magic=142). If a position exists in the same direction, DO NOT execute new sub-orders.
Temporal Blackouts & Weekend Sentry: Block all new entries between 23:55 and 00:15 Broker Server Time. Every Friday at 23:45 Broker Time, halt entries and close open positions. Enter a non-blocking Sentry Loop (time.sleep(60)) on Saturday/Sunday that pings mt5.terminal_info() to maintain the connection.
Fractional Kelly Sizing (CRITICAL): Calculate the raw Kelly Criterion: $f^* = p - (q / b)$. Multiply $f^*$ by KELLY_FRACTION = 0.25 (Quarter-Kelly) to generate the adjusted fraction. All lot sizing must derive exclusively from this fraction.
Absolute Risk Ceilings: Enforce Portfolio Heat (<=20%), Hard Risk Cap (<=2.0% absolute account balance per trade), and the Leverage Wall (<=10x equity margin limit).

Phase 5: Signal Handoff & MCP Execution (Gemini Orchestrator)
The Asynchronous Inbox: The Fast Loop (chat_gemma.py) DOES NOT execute trades directly. Upon a passed 0.82 gate, it formats a clean JSON payload (Symbol, Conviction, HMM State, SL Distance) and drops it into the pending_signals/ directory.
Gemini Commander: A persistent orchestration layer powered by Gemini monitors the inbox.
MCP Tool Routing: Gemini picks up the signal and routes it through the Model Context Protocol (MCP). It triggers the regime_allocator tool to verify the state, and the trade_executor tool to handle the Fractional Kelly math, Sub-Order Routing, and P10/P90 Broker-Side Stops.
Sub-Order Routing & Dynamic Grid Stops: Split capital into 5 sub-orders (1 Market, 4 Limits at 0.5x ATR pullbacks). CRITICAL: Every limit order must calculate its own independent Stop Loss relative to its specific entry price (limit_sl_price = limit_price - sl_distance). Do not assign a single global stop loss price to the grid.
The ATR Volatility Buffer & Stretch Logic: Query trade_stops_level (min 2x spread). Apply a minimum 0.5 * ATR volatility buffer to all final Stop Loss distances to protect against market maker wick-outs. Stretch any tight orders outward to the exact legal boundary to prevent MT5 Error 10016.
Mobile Telemetry: Upon successful MCP execution, the execution tool fires a real-time, timeout-protected webhook alert containing the trade receipt to Discord/Telegram.
P5 Circuit Breaker: If live drawdown >= 15%, throw EMERGENCY_LIQUIDATION=True, close all sub-orders concurrently via ThreadPoolExecutor (deviation 9999), and lock the terminal for 24 hours.
