# Hermes Algorithmic Agents Registry (AGENTS.md)

## 1. Algorithmic Metric Constraints & CodeGraph Rule Mappings
- **The Epistemic Gate:** $\mathcal{W}(P_{live} \parallel P_{train})$ representing the transport distance between 500-bar empirical market states and the training baseline must remain $\le 0.65$. If $> 0.65$, lock out execution with a safe [0.5, 0.5] prediction interval.
- **Sentiment Divergence Delta:** Sentiment serves strictly as a tension coefficient: bs(logit(sentiment_score) - logit(kronos_prob)).
- **FAISS Retrieval Latency Target:** 15ms limit for querying ArcticDB episodic vector memory.
- **Regime-Weighted Time Decay:** FAISS episodic memory matches must be weighted by: exp(-lambda * delta_t) * (1.0 / (1.0 + abs(historic_wasserstein - current_wasserstein)))
- **Allocation Sizing Ceiling:** Kelly scaling factors must invert based on the conformal width: max(0.0, 1.0 - (uncertainty_width * scaling_factor)).

## 2. Delegation Policy (Subagent Sandbox)
- **Tool Mapping:** The native delegate_task function is mapped to the Antigravity invoke_subagent capability.
- **Mandate:** Any parameter sweeping, historical backtesting iteration, or secondary research generated by an SRE tripwire under a PSR degradation halt MUST be offloaded to an isolated subagent session. 
- **Reasoning:** Prevents master context token pollution and isolates volatile execution pathways from the primary execution thread.